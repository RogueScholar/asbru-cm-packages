Description: add whatis entries to the POD
Author: Peter J. Mello <admin@petermello.net>

--- a/xs/Vte.xs
+++ b/xs/Vte.xs
@@ -24,3 +24,3 @@

-=for object Gnome2::Vte::main
+=for object Gnome2::Vte::main Virtual Terminal Emulator library (libvte) version info

--- a/xs/VteTerminal.xs
+++ b/xs/VteTerminal.xs
@@ -68 +68 @@
-			croak ("the pallete parameter must be a reference to an array of GdkColor's");
+			croak ("the palette parameter must be a reference to an array of GdkColor's");
@@ -137 +137,5 @@
-
+
+=for object Gnome2::Vte::Terminal Virtual Terminal Emulator library (libvte) widget
+
+=cut
+
